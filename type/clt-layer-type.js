class CLTLayerType {
    constructor(thickness, angle, materialGrade) {
        this.thickness = thickness; // mm (ti)
        this.angle = angle; // degrees (0 or 90)
        this.materialGrade = materialGrade; // MaterialGrade instance
    }
} 